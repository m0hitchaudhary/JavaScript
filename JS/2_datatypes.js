"use strict" //it is keyword which makes sure that all the code written is as per new standards, but we don't need to write this bc of the advancements in language

// alert(6+6) //this syntax of alert is used when we bind JS with HTML, it returns error in node

let name = "Mohit"
let age = 19
let isLoggedIn = false
let state = null

//null => gives standalone value
//undefined => means that the value has not been assigned
//symbol => uniquely indentfy elements, will work with this in REACT


console.log(typeof null) //returns object
console.log(typeof undefined) //returns undefined
console.log(typeof age) //returns number